{
  "name": "@basalt/demo-data",
  "version": "0.4.0",
  "description": "Example content for Design Systems",
  "main": "src/index.js",
  "scripts": {
    "build:images": "rm -rf public && cp -r images public",
    "build:data": "node src/generateData.js",
    "build": "npm run build:images && npm run build:data",
    "prepare": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "design-systems"
  ],
  "author": "Basalt",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "faker": "^4.1.0"
  }
}
